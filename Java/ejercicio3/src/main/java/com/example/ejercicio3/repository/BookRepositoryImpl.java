package com.example.ejercicio3.repository;

import com.example.ejercicio3.model.Book;
import org.springframework.stereotype.Repository;

@Repository
public class BookRepositoryImpl {// implements BookRepository { // 'implements' era para el ejemplo sin JPA
    /*
    @Override
    public Book getBookById(int id) {
        return new Book(id, "Nuevo libro 1", true);
    }
    */
}
